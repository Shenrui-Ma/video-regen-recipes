--- a/quantization.py
+++ b/quantization.py
@@ -1,3 +1,5 @@
+# Modified by the Heartache template: promote both INT8 output-store indices
+# to tl.int64 before stride multiplication; see ../NOTICE and output-index-int64.patch.
 # SPDX-FileCopyrightText: Copyright (c) 2025 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
 # SPDX-License-Identifier: Apache-2.0
 #
@@ -935,7 +937,7 @@
         c = c + bias[None, :]
 
     # 4. Store Result
-    c_ptrs = c_ptr + stride_cm * offs_am[:, None] + stride_cn * offs_bn[None, :]
+    c_ptrs = c_ptr + stride_cm * offs_am.to(tl.int64)[:, None] + stride_cn * offs_bn.to(tl.int64)[None, :]
     c_mask = (offs_am[:, None] < m) & (offs_bn[None, :] < n)
     tl.store(c_ptrs, c, mask=c_mask)
 
@@ -1012,7 +1014,7 @@
         c = c + bias[None, :]
 
     # 4. Store Result
-    c_ptrs = c_ptr + stride_cm * offs_am[:, None] + stride_cn * offs_bn[None, :]
+    c_ptrs = c_ptr + stride_cm * offs_am.to(tl.int64)[:, None] + stride_cn * offs_bn.to(tl.int64)[None, :]
     c_mask = (offs_am[:, None] < m) & (offs_bn[None, :] < n)
     tl.store(c_ptrs, c, mask=c_mask)
 
