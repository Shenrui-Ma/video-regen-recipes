# 历史证据与本次验证边界

## 历史中文示例

以下均是资料包内已有日志、清单和只读补核记录，不是本模板重新训练的结果。角色是莫斯提马（Mostima，明日方舟），语言 zh-CN，原配音若舞；同角色日配支线不能改名当中文模型。

| 项目 | 历史记录 | 限制 |
| --- | --- | --- |
| 来源 | BV17a411d7ex，完整合集 246.677333 s | 转载整理，不是官方账号或训练许可 |
| 解码源 | 48k 双声道 PCM16，47,362,126 bytes | 有损平台音源转 PCM 不代表无损原始录音 |
| 源 SHA-256 | `1e607b898e732599cc79b20f05e2d91721c801e4f4230164955b6f70cc22301f` | 源包记载的字节结果，此次未读取源音频 |
| 全部预处理 | 87 个 48k FLOAT、87 个 16k FLOAT，194.82 s | 有重叠，非去重语音覆盖；不把 `.spec.pt` 计入 WAV |
| F0/NSF | 各 86 份，0_61.wav 全零 F0 跳过 | 被排除片段 0.62 s，不证明其 HuBERT 缺失 |
| 四路交集 | 86 角色行、194.20 s，加 2 mute=88 行 | mute 不计角色时长；不是 86 行总计含 mute |
| HuBERT/索引 | 87 份，矩阵 9684 x 768，IVF248/nprobe1 | 索引用全部 HuBERT，不是 86 行 F0 训练交集 |
| 训练参数 | v2/48k/F0，batch16、200 epoch、每50保存 | 模板 config 的默认 epochs/batch 不能代替 CLI |
| 保存记录 | e50_s400、e100_s800、e150_s1200、e200_s1600 | 依据保存日志/文件记录，未加载内部 state |
| 实际选择 | e150_s1200 | 脚本硬编码选择，无独立验证集或完整听审矩阵，不证明最佳 |
| 训练日志时间 | 2026-08-01 02:27:24 hparams，02:37:17 completed | 原日志时区未独立核验；名称里的 20260731 不等于精确执行日期 |

历史小模型 57,599,275 bytes，SHA-256 `1e6fa20f438d916e9d64b2b9c48d16dbe0c4f689eab1d461a58e3f0b6934acdf`；历史 added 索引 30,590,699 bytes，SHA-256 `30c1752125e09074bbc4d72e297706204cdbc538a7d8c13f3dcbb1fa72c690c5`。只保留识别指纹，不附文件、下载地址或再分发授权；不要求新角色训练得到相同 SHA。

中文历史源合集没有证据证明训练前已做逐句声纹验证、人工去重、语义剪辑、BGM 分离、独立留出或完整 F0 覆盖统计。新模板给出的筛选和分组留出是改进流程，不能写成历史已执行。原合集到各预处理片段的精确区间与逐片 hash 未完整恢复；不能编造无泄漏统计。

历史输出资料记录 RVC 翻唱分支，但部分 converted stems/premaster 被旧脚本清理，无法据此承诺完整重做历史混音。没有已独立绑定到该中文模型和最终母带哈希的公开成品 URL，因此本模板不挂其他角色或旧日配成品替代。公开视频和音频字节一致性需另证。

## 此次离线检查

从仓库根执行：

```bash
python3 -B -m unittest discover -s templates/singing-cover/character-rvc/tests -p 'test_helpers.py' -v
python3 templates/singing-cover/character-rvc/scripts/build_filelist.py --help
python3 templates/singing-cover/character-rvc/scripts/run_guard.py --help
```

本次主体测试为 **8 项通过**；发现模块完成后，执行全目录离线测试的最终快照结果为 **24 项通过（发现模块 16 项 + 主体 helper 8 项）**：

```bash
python3 -B -m unittest discover -s templates/singing-cover/character-rvc/tests -v
```

另完成目录内 5 份 Python 的 AST 解析、3 份 JSON 解析、9 份 Markdown 内部链接及 Python 示例语法检查，扫描无私有路径、危险清理命令或尾随空白。`model-catalog/dependencies.json` 未被 gitignore 忽略。新增 Python 的 Ruff 检查通过；没有做完整静态类型分析。

`tests/test_helpers.py` 用临时目录和合成字节验证：四路交集、额外 HuBERT、缺失模态显式接受、2 mute 分列、确定性排序/洗牌、`.spec.pt` 排除、空输入/坏路径/符号链接/覆盖拒绝、目录成员与参数/同大小内容变化、receipt 自引用和篡改检测。临时“媒体”不是有效 WAV/NPY，刻意只测试文件集合与哈希协议，不作为解码或训练成功证据。

来源发现脚本的行为与工具要求见[来源发现](discovery.md)。测试数量不是已核验的官方素材数量；模拟元数据测试不证明站点在线可用。

## 此次有限联网验证

本次在隔离 uv 工具环境使用 yt-dlp **2026.08.19**。Bilibili 元数据查询返回 3 条未能解析为完整候选的条目，结合该版本提取器源码确认其 flat 搜索只生成 URL 引用；随后补上 URL-only 候选处理、相对可执行路径和分 P 保留的回归测试。后续 Bilibili 返回 **HTTP 412**，已停止该站尝试，未取得修复后的在线候选详情，不声称下载或全链路成功。YouTube 有界查询正常返回 **0 候选**，只代表该次查询，不代表全站或全网不存在目标语音。

Agent 原生网页搜索成功找到 PRTS 莫斯提马语音记录页，多语言文本可读；**不等于全部中文音频下载链接已确认**，也不证明录音身份、来源官方性或训练/发布权限全部通过。没有下载原语音、权重或启动训练。这些有限成功和平台阻断应一起保留，不能写成“全网络来源搜索成功”。

## 未执行与验收门槛

此次未下载语音或模型、未 SSH、未部署 Linux GPU、未做模型加载/训练/FAISS 反序列化、未执行推理示例、未混音或听审。也未对源包中媒体/权重/索引重新算 hash；依赖清单指纹来自包内核验记录，本次另核对了 ZIP 和 payload 完整性。源包只读，旧脚本只作证据。

标准库 helper 不检查 NPY 数值/形状、WAV 解码、音域覆盖、分组泄漏、checkpoint 内容、优化器恢复、索引内部维度/ntotal、授权、进程锁或声音质量。技术示例经固定源码核对，不等于已在新环境通过。没有依赖 lock/可信权重、真实授权输入和听审时，停止在对应阶段，不能以占位文件代替。

新角色完整验收至少包括：本次来源/用途依据、真实筛选和分组、部署与资产校验、逐阶段日志及输入输出哈希、音频/数组技术检查、候选 checkpoint 与索引配对、短段和全曲听审、母带独立响度/真峰值测量、可恢复的干声/工程和交付清单。只有这次实际完成的项才能标通过。
