# 参与 Video ReGen Recipes

欢迎贡献二次元视频拍法、角色与声音准备方法、ComfyUI 工作流，以及真实复现中的问题与修复。

当前先聚焦 **本地或自托管 MiniMax H3 视频推理模板**。图像、语音、音乐和其他辅助素材可以来自本地工具、在线 API、手工作品或授权素材。请在配方里说明实际使用的工具与数据流。

## 可以从哪里开始

- 在 Issue 中推荐有明确作者和方法来源的视频案例。
- 补充角色参考图、场景图、音色或关键帧的准备方法。
- 提交一个完整的 H3 制作配方，并记录环境和实际结果。
- 用不同角色复现已有配方，说明修改和返工过程。
- 修正失效的节点、路径、参数或文档。

仅有原视频或提示词时，可以先作为案例线索提交；不要把它标为已经验证的模板。

## 模板提交内容

请参考 [模板格式](docs/template-format.md)，至少包含：

1. **效果与来源**：想实现什么、原作者和参考链接、制作方法及素材许可。
2. **输入与可改范围**：角色、场景、声音、动作或关键帧要求，以及效果依赖的规则。
3. **完整制作过程**：素材准备、分镜、H3 输入、候选选择、音轨与剪辑。
4. **实际执行配置**：模型、节点、软件版本、精度、采样、分辨率、帧数与硬件。
5. **验证记录**：运行日期、结果、失败原因、耗时、资源或费用口径，以及仍未验证的条件。

可以只支持一条经过验证的本地 H3 配置。不同图像或语音方案属于可选组合，不要求一次覆盖所有工具和 Agent。

## 保留原稿与实际输入

作者原提示词、修改后的提交稿、当前参考编号及文件对应关系应分别记录。GUI 工作流的保存状态不等于执行状态；导出 API 图前应检查旁路、连线覆盖、模型与素材依赖。

视频中的镜头、一次生成的片段和最终剪辑时间线分别管理。局部修改时说明哪些片段被重做、哪些素材被复用。

## 素材与许可

仓库自有代码和文档采用 [MIT License](LICENSE)。第三方代码、工作流、模型和素材保留各自许可，不因进入本仓库而重新获得授权。

请只提交可再分发的素材。缺少明确许可时，保留原始链接与自己撰写的方法摘要。声音参考使用自有或已获授权的录音，并注明它用于音色参考还是实际音轨复用。

不要提交账号凭证、cookies、私人绝对路径、模型权重、私人角色库或生成缓存。示例优先使用体积较小、来源明确的文件。

## 提交前检查

- 文档链接可以打开，目录和文件名一致。
- 实测与推测分开，未测试的条件明确标注。
- 模型和节点名称对应真实配置，图片与声音输入绑定正确。
- 示例使用真实测量值，没有把目标时长当实际输出时长。
- 素材能够公开，文件不含私人信息。

PR 请说明具体模板、改动原因、验证方式和已知限制。首次贡献不需要先实现完整番剧；清楚、可复核的一段制作经验同样有价值。

